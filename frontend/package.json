{
  "name": "dance-driven-ai-music",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy-mediapipe-assets": "mkdir -p public/mediapipe/wasm && cp -R node_modules/@mediapipe/tasks-vision/wasm/* public/mediapipe/wasm/",
    "download-mediapipe-models": "node scripts/download-mediapipe-models.js",
    "prepare-offline": "npm run copy-mediapipe-assets && npm run download-mediapipe-models"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
